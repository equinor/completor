name: "📚 Check Docs"

on:
  push:
    paths:
      - "documentation/**"
      - "!README.*"

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: install
        run: yarn install

      - name: build
        run: yarn build
